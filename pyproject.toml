[project]
name = "layoutnotification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "customtkinter>=5.2.2",
    "pywin32>=311",
]

[tool.pyright]

venvPath = "."
venv = ".venv"
